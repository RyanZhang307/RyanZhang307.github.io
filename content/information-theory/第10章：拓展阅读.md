# Appendices A-H

# 附录 A-H

## Appendix A: Glossary / 附录 A：术语表

| Term | English explanation | 中文术语 | 中文解释 |
|---|---|---|---|
| alphabet | The set of symbols a variable or message may use. | 字母表 / 符号集 | 一个变量或消息可以使用的全部符号所组成的集合。 |
| average | A representative central value. For observations, it is the sample mean; for a distribution, it is the expected value. | 平均值 | 表示数据中心位置的数值。对样本是算术平均，对分布是期望值。 |
| Bayes' rule | A rule that reverses a conditional probability by combining likelihood, prior and marginal likelihood. | 贝叶斯规则 | 结合似然、先验概率和边缘似然，以反转条件概率的规则。 |
| binary digit | One of the two digits 0 and 1. | 二进制数字 | 0 或 1 这两个数字之一。 |
| binary number | A number represented using powers of 2. | 二进制数 | 使用 2 的各次幂表示的数。 |
| binary symmetric channel | A binary channel in which either input bit has the same probability of being flipped. | 二元对称信道 | 输入 0 或 1 时具有相同翻转概率的二元信道。 |
| binomial coefficient | The number of ways to choose (k) unordered items from (n) items. | 二项式系数 | 从 (n) 个元素中无序选择 (k) 个元素的方法数。 |
| bit | The unit of information obtained when a base-2 logarithm is used. | 比特 | 使用以 2 为底的对数时得到的信息量单位。 |
| byte | A group of eight binary digits. | 字节 | 由 8 个二进制数字组成的单位。 |
| capacity | The greatest reliable information rate supported by a channel. | 信道容量 | 信道能够支持的最大可靠信息传输速率。 |
| channel | A physical or abstract medium that maps an input to an output, possibly with noise. | 信道 | 把输入映射为输出的物理或抽象媒介，过程中可能含有噪声。 |
| code | A rule for representing source messages with codewords. | 编码 | 用码字表示信源消息的规则。 |
| codebook | The complete mapping between messages and codewords. | 码本 | 消息与码字之间全部对应关系的集合。 |
| codeword | A particular sequence used to represent a message. | 码字 | 用来表示某条消息的特定符号序列。 |
| coding efficiency | How closely a code approaches the relevant theoretical limit. | 编码效率 | 编码性能接近相应理论极限的程度。 |
| conditional probability | The probability of one event given that another is known to have occurred. | 条件概率 | 已知另一事件发生时，某事件发生的概率。 |
| conditional entropy | The average uncertainty remaining in one variable after another is observed. | 条件熵 | 观察到另一个变量后，目标变量中平均剩余的不确定性。 |
| continuous | Able to take any value in an interval. | 连续的 | 能够取某个区间内任意数值的。 |
| cumulative distribution function | The probability that a random variable is no greater than a specified value. | 累积分布函数 | 随机变量不大于指定数值的概率。 |
| differential entropy | The continuous-variable analogue of discrete entropy. | 微分熵 | 离散熵在连续变量情形下的对应量。 |
| discrete | Able to take values from a countable set. | 离散的 | 只能从可数集合中取值的。 |
| disjoint | Having no outcome in common. | 互斥的 / 不相交的 | 两个事件没有共同结果。 |
| encoding | Transforming a message into a representation suitable for storage or transmission. | 编码过程 | 把消息转换成适合存储或传输的表示。 |
| ensemble | A conceptual collection of possible systems, samples or messages governed by one distribution. | 系综 | 由同一概率分布支配的一组可能系统、样本或消息。 |
| entropy | The expected Shannon information of a random variable. | 熵 | 随机变量的香农信息量的期望值。 |
| expected value | A probability-weighted average over all possible values. | 期望值 | 对全部可能取值按其概率加权所得的平均值。 |
| histogram | Counts or relative frequencies grouped into finite-width bins. | 直方图 | 把观测值分箱后显示各箱计数或相对频率的图。 |
| iid | Independent and identically distributed. | 独立同分布 | 各随机变量彼此独立并具有相同概率分布。 |
| independence | Knowledge of one outcome does not change the probability of another. | 独立性 | 知道一个结果不会改变另一个结果的概率。 |
| information | A reduction in uncertainty; for an outcome (x), its Shannon information is (log_2[1/p(x)]). | 信息 | 不确定性的减少；结果 (x) 的香农信息量为 (log_2[1/p(x)])。 |
| integration | The continuous counterpart of summation, often interpreted as area under a curve. | 积分 | 求和在连续情形下的对应运算，常可解释为曲线下面积。 |
| joint probability | The probability that specified values of two or more variables occur together. | 联合概率 | 两个或多个变量同时取指定数值的概率。 |
| Kolmogorov complexity | The length of the shortest program that generates a specified object. | 柯尔莫哥洛夫复杂度 | 能生成指定对象的最短程序长度。 |
| Kullback-Leibler divergence | The expected log-ratio between two distributions; a directed measure of their difference. | Kullback-Leibler 散度 | 两个分布概率比的对数期望，是一种有方向的分布差异度量。 |
| law of large numbers | Sample averages approach the population expectation as sample size grows. | 大数定律 | 当样本量增大时，样本平均趋近期望值。 |
| logarithm | The exponent to which a base must be raised to produce a number. | 对数 | 为得到某个数而必须把底数提升到的幂次。 |
| mean | Another name for an average or expected value. | 均值 | 平均值或期望值的另一种说法。 |
| message | A sequence of source symbols intended for communication. | 消息 | 准备用于通信的一串信源符号。 |
| marginal distribution | The distribution of one variable obtained by summing or integrating a joint distribution over the others. | 边缘分布 | 对联合分布中的其他变量求和或积分后得到的单变量分布。 |
| monotonic | Always non-decreasing or always non-increasing. | 单调的 | 始终不减或始终不增的。 |
| mutual information | The average information one variable provides about another. | 互信息 | 一个变量平均向另一个变量提供的信息量。 |
| noise | Unwanted randomness or disturbance that changes a signal or observation. | 噪声 | 改变信号或观测值的非期望随机扰动。 |
| outcome | One possible result of an experiment or random process. | 结果 | 一次实验或随机过程可能出现的某个结果。 |
| outcome value | The numerical or symbolic value associated with an outcome. | 结果值 | 与某个结果对应的数值或符号。 |
| outer product | A product of two vectors that produces a matrix of all pairwise component products. | 外积 | 两个向量相乘并生成全部分量两两乘积矩阵的运算。 |
| parity | Whether the number of 1s in a binary sequence is even or odd. | 奇偶校验 | 二进制序列中 1 的数量为偶数还是奇数。 |
| precision | The fineness or number of distinguishable levels in a representation or measurement. | 精度 | 表示或测量所能区分的细致程度或层级数量。 |
| prefix code | A code in which no codeword is the prefix of another. | 前缀码 | 任一码字都不是另一码字前缀的编码。 |
| probability | A number between 0 and 1 expressing uncertainty about an outcome; it may be interpreted through belief or long-run frequency. | 概率 | 0 到 1 之间、表示结果不确定性的数；可解释为信念程度或长期频率。 |
| probability density function | A non-negative function whose integral over a region gives the probability of that region and whose total integral is 1. | 概率密度函数 | 在某区间上的积分给出该区间概率、且全域积分为 1 的非负函数。 |
| probability distribution | A specification of probabilities for all possible values of a random variable. | 概率分布 | 为随机变量全部可能取值指定概率的规则。 |
| probability function | A function assigning probabilities to the values of a discrete random variable. | 概率函数 | 为离散随机变量各取值分配概率的函数。 |
| quantum computer | A computer that processes quantum states and can exploit superposition and interference. | 量子计算机 | 处理量子态并可利用叠加与干涉的计算机。 |
| random variable | A variable whose possible values have specified probabilities. | 随机变量 | 各可能取值由概率分布规定的变量。 |
| redundancy | Predictable structure beyond the minimum information needed to specify a message. | 冗余 | 超出确定消息所需最少信息量的可预测结构。 |
| relative entropy | Another name for Kullback-Leibler divergence. | 相对熵 | Kullback-Leibler 散度的另一名称。 |
| relative frequency | The number of occurrences divided by the total number of trials. | 相对频率 | 某结果出现次数除以试验总次数。 |
| sample space | The set of all possible outcomes. | 样本空间 | 全部可能结果组成的集合。 |
| standard deviation | The square root of variance; a measure of spread in the units of the variable. | 标准差 | 方差的平方根，以变量原单位度量分散程度。 |
| stationary source | A source whose probability law does not change with time. | 平稳信源 | 概率规律不随时间改变的信源。 |
| symbol | An elementary member of an alphabet. | 符号 | 字母表中的基本元素。 |
| theorem | A proposition established from stated assumptions by logical argument. | 定理 | 从明确假设出发，经逻辑论证成立的命题。 |
| uncertainty | Lack of knowledge about which outcome will occur. | 不确定性 | 对将出现哪个结果缺乏确定知识。 |
| variable | A quantity or symbol that may take different values. | 变量 | 可以取不同数值的量或符号。 |
| variance | The expected squared deviation from the mean. | 方差 | 随机变量相对均值的平方偏差的期望。 |

The principal glossary equations are:

> 术语表中的主要公式如下：

$$
\bar{x}=\frac{1}{n}\sum_{i=1}^{n}x_i, \qquad
E[X]=\sum_{i=1}^{m}p(x_i)x_i. \tag{A.1-A.2}
$$

$$
{n\choose k}=\frac{n!}{k!(n-k)!}. \tag{A.3}
$$

$$
H(X)=\sum_{i=1}^{m}p(x_i)\log_2\frac{1}{p(x_i)}. \tag{A.4}
$$

$$
D_{\mathrm{KL}}(P\|Q)=\sum_i p(x_i)\log_2\frac{p(x_i)}{q(x_i)}. \tag{A.5}
$$

$$
v_X=E[(X-\mu)^2]=E[X^2]-\mu^2, \qquad \sigma_X=\sqrt{v_X}. \tag{A.6-A.7}
$$

## Appendix B: Mathematical Symbols / 附录 B：数学符号

| Symbol          | English meaning                                            | 中文含义                    |
| --------------- | ---------------------------------------------------------- | ----------------------- |
| (hat{x})        | An estimated value of (x).                                 | (x) 的估计值。               |
| (lvert x\rvert) | Absolute value of (x).                                     | (x) 的绝对值。               |
| (leq,geq)       | Less than or equal to; greater than or equal to.           | 小于等于；大于等于。              |
| (approx)        | Approximately equal to.                                    | 约等于。                    |
| (X\sim p(X))    | Random variable (X) has distribution (p(X)).               | 随机变量 (X) 服从分布 (p(X))。   |
| (infty)         | Infinity.                                                  | 无穷大。                    |
| (propto)        | Proportional to.                                           | 正比于。                    |
| (alpha)         | Number of different symbols in an alphabet.                | 字母表中不同符号的数量。            |
| (Delta)         | A finite increment, such as bin width (Delta x).           | 有限增量，例如箱宽 (Delta x)。    |
| (epsilon)       | A small quantity.                                          | 小量。                     |
| (eta, H)        | A noise value and, where defined, a noise random variable. | 一个噪声取值，以及按上下文定义的噪声随机变量。 |
| (mu)            | Mean value.                                                | 均值。                     |
|                 |                                                            |                         |
ho) | Correlation. | 相关性。 |
| (sigma) | Standard deviation. | 标准差。 |
| (Sigma) | Summation. | 求和。 |
| (mathcal A_X) | Alphabet or set of possible values of (X). | (X) 的字母表或可能取值集合。 |
| (C) | Channel capacity. | 信道容量。 |
| (e) | Base of natural logarithms, approximately (2.71828). | 自然对数的底，约为 (2.71828)。 |
| (E[X]) | Expected value of (X). | (X) 的期望值。 |
| (g) | Encoding function, for example (mathbf{x}=g(mathbf{s})). | 编码函数，例如 (mathbf{x}=g(mathbf{s}))。 |
| (h(x)) | Shannon information of outcome (x). | 结果 (x) 的香农信息量。 |
| (ar h(x)) | Average Shannon information in a finite sample. | 有限样本中的平均香农信息量。 |
| (H(X)) | Entropy of (X). | (X) 的熵。 |
| (H(X\mid Y)) | Conditional entropy of (X) given (Y). | 给定 (Y) 时 (X) 的条件熵。 |
| (H(X,Y)) | Joint entropy of (X) and (Y). | (X) 与 (Y) 的联合熵。 |
| (I(X,Y)) | Mutual information between (X) and (Y). | (X) 与 (Y) 的互信息。 |
| (ln x,log_2x) | Natural logarithm and base-2 logarithm. Unqualified logarithms in the book use base 2. | 自然对数和以 2 为底的对数；本书未标底的对数采用底 2。 |
| (m) | Number of possible messages, codewords or alphabet symbols. | 可能消息、码字或字母表符号的数量。 |
| (M) | Number of histogram bins. | 直方图箱数。 |
| (N) | Noise variance in the Gaussian-channel capacity equation. | 高斯信道容量公式中的噪声方差。 |
| (n) | Number of observations or elements in a sequence. | 观测数或序列元素数。 |
| (p(X),p(x)) | Distribution of (X); probability or density at (X=x). | (X) 的分布；在 (X=x) 处的概率或密度。 |
| (p(X,Y),p(x,y)) | Joint distribution; joint probability or density at (x,y). | 联合分布；在 (x,y) 处的联合概率或密度。 |
| (p(x\mid y)) | Conditional probability of (x) given (y). | 给定 (y) 时 (x) 的条件概率。 |
| (R) | Information rate. | 信息传输速率。 |
| (S,s,mathbf{s}) | Message random variable, one message value and a message vector. | 消息随机变量、一个消息取值和消息向量。 |
| (v_X) | Variance of (X). | (X) 的方差。 |
| (W) | Total number of microstates. | 微观状态总数。 |
| (X,x,mathbf{x}) | Random variable, one value and a vector of values. | 随机变量、一个取值和取值向量。 |
| (X_{\Delta}) | A variable quantised into intervals of width (Delta x). | 量化到宽度为 (Delta x) 的区间中的变量。 |
| (Y,y) | Random variable and one of its values, often a channel output. | 随机变量及其一个取值，常表示信道输出。 |

## Appendix C: Logarithms / 附录 C：对数

A logarithm states the power to which a base must be raised. Since (10^2=100), (log_{10}100=2). The inverse operation is exponentiation: if (y=log_a x), then (x=a^y).

> 对数表示底数必须提升到的幂次。因为 (10^2=100)，所以 (log_{10}100=2)。其逆运算是乘方：若 (y=log_a x)，则 (x=a^y)。

Logarithms are useful because they turn multiplication into addition:

> 对数的重要作用是把乘法转换为加法：

$$
\log_b(nm)=\log_b n+\log_b m, \tag{C.6}
$$

$$
nm=b^{\log_b n+\log_b m}. \tag{C.7}
$$

The change-of-base rule is

> 换底公式为：

$$
\log_b x=\frac{\log_a x}{\log_a b}. \tag{C.8}
$$

Natural logarithms have base (e) and are written (ln x). Unless otherwise stated, the book uses base-2 logarithms. Figure C.1 compares the monotonic logarithmic functions with bases 2 and 10.

> 自然对数以 (e) 为底，写作 (ln x)。除非另有说明，本书采用以 2 为底的对数。图 C.1 比较了以 2 和 10 为底的单调对数函数。

## Appendix D: Probability Density Functions / 附录 D：概率密度函数

A function maps values from a domain to a range. A probability density function (pdf) maps every possible value of a continuous variable to a non-negative density. Probability is area, not the height at a single point.

> 函数把定义域中的值映射到值域。概率密度函数把连续变量的每个可能取值映射为非负密度。概率对应曲线下面积，而不是某一点的曲线高度。

A histogram approximates a pdf. Divide observations into (M) bins of width (Delta x), let (a_i) be the area of bin (i), and let (A) be the total area:

> 直方图可以近似概率密度函数。把观测值分入 (M) 个宽度为 (Delta x) 的箱，令 (a_i) 为第 (i) 个箱的面积，(A) 为总面积：

$$
A=\sum_{i=1}^{M}a_i. \tag{D.1}
$$

The normalised bin probabilities sum to 1, and the width of a bin is

> 归一化后的各箱概率之和为 1，箱宽为：

$$
\sum_{i=1}^{M}P_i=1, \qquad \Delta x=x_i^{\max}-x_i^{\min}. \tag{D.2-D.3}
$$

For bin (i), probability equals area and density equals area divided by width:

> 对第 (i) 个箱，概率等于面积，而密度等于面积除以宽度：

$$
P_i=p(x_i)\Delta x, \qquad p(x_i)=\frac{P_i}{\Delta x}. \tag{D.4-D.6}
$$

As (Delta x) approaches zero, the sum becomes an integral. A valid pdf satisfies

> 当 (Delta x) 趋于零时，求和变为积分。有效的概率密度函数满足：

$$
\sum_{i=1}^{M}p(x_i)\Delta x\longrightarrow \int_{\mathcal X}p(x)\,dx=1. \tag{D.7-D.8}
$$

Figure D.1 shows a normalised histogram approaching a smooth pdf as bins become thinner. Figure D.2 shows that the probability of an interval is the area between its boundaries and the pdf curve.

> 图 D.1 展示归一化直方图如何在箱变窄时逼近平滑的概率密度函数。图 D.2 展示某区间的概率就是该区间边界与密度曲线围成的面积。

## Appendix E: Averages From Distributions / 附录 E：从分布求平均值

For a discrete variable, the mean, average and expected value are the probability-weighted sum of possible values:

> 对离散变量，均值、平均值和期望值都是各可能取值按概率加权后的总和：

$$
E[X]=\sum_{i=1}^{m}p(x_i)x_i. \tag{E.1}
$$

For example, values (4,5,6) with probabilities (0.1,0.2,0.7) have expectation

> 例如，取值 (4,5,6) 的概率分别为 (0.1,0.2,0.7)，其期望为：

$$
E[X]=(0.1)(4)+(0.2)(5)+(0.7)(6)=5.6. \tag{E.2-E.5}
$$

More generally, the expected value of a function (h(X)) is

> 更一般地，函数 (h(X)) 的期望为：

$$
E[h(X)]=\sum_{i=1}^{m}p(x_i)h(x_i). \tag{E.6-E.9}
$$

If (h(x)=\log_2[1/p(x)]), this expectation is entropy:

> 若 (h(x)=\log_2[1/p(x)])，这个期望就是熵：

$$
E[h(X)]=\sum_i p(x_i)\log_2\frac{1}{p(x_i)}=H(X). \tag{E.10-E.11}
$$

For a continuous variable, summation is replaced by integration:

> 对连续变量，求和由积分代替：

$$
E[h(X)]=\int_{\mathcal X}p(x)h(x)\,dx. \tag{E.12}
$$

Choosing Shannon information for (h(x)) gives differential entropy:

> 令 (h(x)) 为香农信息量，即得到微分熵：

$$
H(X)=\int_{\mathcal X}p(x)\log_2\frac{1}{p(x)}\,dx. \tag{E.13-E.14}
$$

## Appendix F: The Rules of Probability / 附录 F：概率规则

### Independent Outcomes / 独立结果

If two outcomes are independent, their joint probability is the product of their individual probabilities:

> 若两个结果彼此独立，其联合概率等于各自概率的乘积：

$$
p(x,y)=p(x)p(y).
$$

For a coin with (p(h)=0.9) and (p(t)=0.1), two flips give (p(h,h)=0.81), (p(t,t)=0.01), and a head and tail in either order with probability (0.18). Table F.1 expresses the expected counts out of 100 pairs and distinguishes ordered sequences ((h,t)) from unordered combinations ({h,t}).

> 对一枚 (p(h)=0.9)、(p(t)=0.1) 的硬币，连续投掷两次得到双正面的概率为 (0.81)，双反面的概率为 (0.01)，一正一反而不计顺序的概率为 (0.18)。表 F.1 用 100 对投掷的期望计数说明这些结果，并区分有序序列 ((h,t)) 与无序组合 ({h,t})。

### Conditional Probability and Product Rule / 条件概率与乘法规则

$$
p(x\mid y)=\frac{p(x,y)}{p(y)}, \tag{F.4}
$$

$$
p(x,y)=p(x\mid y)p(y). \tag{F.5}
$$

The vertical bar is read as "given". The product rule follows directly by multiplying the conditional-probability definition by (p(y)).

> 竖线读作“给定”。把条件概率定义的两边乘以 (p(y))，就得到乘法规则。

### Sum Rule and Marginalisation / 求和规则与边缘化

For a discrete variable,

> 对离散变量：

$$
p(x)=\sum_i p(x,y_i)=\sum_i p(x\mid y_i)p(y_i). \tag{F.6-F.7}
$$

For a continuous variable,

> 对连续变量：

$$
p(x)=\int p(x,y)\,dy=\int p(x\mid y)p(y)\,dy. \tag{F.8}
$$

This operation removes (Y) from the joint distribution and produces the marginal probability of (X).

> 这个运算从联合分布中消去 (Y)，得到 (X) 的边缘概率。

### Bayes' Rule / 贝叶斯规则

Because (p(y,x)=p(x,y)), the two product-rule expressions can be equated:

> 因为 (p(y,x)=p(x,y))，可以令乘法规则的两种写法相等：

$$
p(y\mid x)p(x)=p(x\mid y)p(y). \tag{F.9-F.10}
$$

Therefore,

> 因此：

$$
p(y\mid x)=\frac{p(x\mid y)p(y)}{p(x)}. \tag{F.11}
$$

In Bayesian terminology, (p(y\mid x)) is the posterior, (p(x\mid y)) the likelihood, (p(y)) the prior, and (p(x)) the marginal likelihood or evidence. The same relation applies to complete distributions:

> 在贝叶斯术语中，(p(y\mid x)) 是后验概率，(p(x\mid y)) 是似然，(p(y)) 是先验概率，(p(x)) 是边缘似然或证据。同一关系也适用于完整分布：

$$
p(Y\mid X)=\frac{p(X\mid Y)p(Y)}{p(X)}. \tag{F.12}
$$

## Appendix G: The Gaussian Distribution / 附录 G：高斯分布

A Gaussian probability density function, also called a normal distribution or bell curve, with mean (mu) and standard deviation (sigma) is

> 均值为 (mu)、标准差为 (sigma) 的高斯概率密度函数，也称正态分布或钟形曲线：

$$
p(x)=\frac{1}{\sigma\sqrt{2\pi}}\exp\left[-\frac{(x-\mu)^2}{2\sigma^2}\right]. \tag{G.1}
$$

Writing

> 令：

$$
z=\frac{(x-\mu)^2}{2\sigma^2}, \tag{G.2}
$$

gives (p(x)=ke^{-z}), where (k=1/(\sigma\sqrt{2\pi})). The density is greatest at (x=\mu). Changing (mu) shifts the curve, while increasing (sigma) spreads it out. Figure G.1(a) shows the standard Gaussian with (mu=0) and (sigma=1).

> 于是 (p(x)=ke^{-z})，其中 (k=1/(\sigma\sqrt{2\pi}))。密度在 (x=\mu) 处最大。改变 (mu) 会平移曲线，增大 (sigma) 会使曲线更分散。图 G.1(a) 展示 (mu=0,sigma=1) 的标准高斯分布。

### The Central Limit Theorem / 中心极限定理

As the number of observations in each sample grows, the distribution of sample means drawn from almost any parent distribution becomes increasingly Gaussian. Many physical quantities are affected by multiple factors and are therefore weighted sums or averages, which helps explain the prevalence of Gaussian distributions in nature.

> 随着每个样本中的观测数量增大，从几乎任意母分布得到的样本均值分布会越来越接近高斯分布。许多物理量受多个因素共同影响，实质上是加权和或平均，这有助于解释高斯分布为何在自然界中普遍出现。

### Cumulative Distribution Function / 累积分布函数

The Gaussian cumulative distribution function is the area under the pdf from negative infinity to (x):

> 高斯累积分布函数是从负无穷到 (x) 的概率密度曲线下面积：

$$
\Phi(x)=\frac{1}{\sigma\sqrt{2\pi}}\int_{-\infty}^{x}
\exp\left[-\frac{(t-\mu)^2}{2\sigma^2}\right]dt. \tag{G.3}
$$

Thus (Phi(x)=P(X\leq x)). Figure G.1(b) plots this cumulative function for the standard Gaussian.

> 因而 (Phi(x)=P(X\leq x))。图 G.1(b) 绘出了标准高斯分布的累积分布函数。

## Appendix H: Key Equations / 附录 H：关键公式

### Entropy / 熵

For a discrete random variable,

> 对离散随机变量：

$$
H(X)=\sum_{i=1}^{m}p(x_i)\log_2\frac{1}{p(x_i)}. \tag{H.1}
$$

For a continuous random variable,

> 对连续随机变量：

$$
H(X)=\int_{\mathcal X}p(x)\log_2\frac{1}{p(x)}\,dx. \tag{H.2}
$$

### Joint Entropy / 联合熵

$$
H(X,Y)=\sum_{i=1}^{m}\sum_{j=1}^{n}p(x_i,y_j)
\log_2\frac{1}{p(x_i,y_j)}, \tag{H.3}
$$

$$
H(X,Y)=\int_{\mathcal X}\int_{\mathcal Y}p(x,y)
\log_2\frac{1}{p(x,y)}\,dy\,dx. \tag{H.4}
$$

The information diagram identity is

> 信息图中的恒等式为：

$$
H(X,Y)=I(X,Y)+H(X\mid Y)+H(Y\mid X). \tag{H.5}
$$

### Conditional Entropy / 条件熵

$$
H(Y\mid X)=\sum_i\sum_j p(x_i,y_j)
\log_2\frac{1}{p(y_j\mid x_i)}, \tag{H.6}
$$

$$
H(X\mid Y)=\sum_i\sum_j p(x_i,y_j)
\log_2\frac{1}{p(x_i\mid y_j)}. \tag{H.7}
$$

For continuous variables, replace the sums by integrals:

> 对连续变量，把求和替换为积分：

$$
H(X\mid Y)=\int\!\!\int p(x,y)\log_2\frac{1}{p(x\mid y)}\,dx\,dy, \tag{H.8}
$$

$$
H(Y\mid X)=\int\!\!\int p(x,y)\log_2\frac{1}{p(y\mid x)}\,dx\,dy. \tag{H.9}
$$

Equivalent identities and the chain rule are

> 等价恒等式与熵的链式法则为：

$$
H(X\mid Y)=H(X,Y)-H(Y), \tag{H.10}
$$

$$
H(Y\mid X)=H(X,Y)-H(X), \tag{H.11}
$$

$$
H(X,Y)=H(X)+H(Y\mid X)=H(Y)+H(X\mid Y). \tag{H.12}
$$

### Mutual Information / 互信息

$$
I(X,Y)=\sum_i\sum_j p(x_i,y_j)
\log_2\frac{p(x_i,y_j)}{p(x_i)p(y_j)}, \tag{H.13}
$$

$$
I(X,Y)=\int\!\!\int p(x,y)
\log_2\frac{p(x,y)}{p(x)p(y)}\,dx\,dy. \tag{H.14}
$$

Equivalent forms are

> 等价形式为：

$$
I(X,Y)=H(X)+H(Y)-H(X,Y), \tag{H.15}
$$

$$
I(X,Y)=H(X)-H(X\mid Y), \tag{H.16}
$$

$$
I(X,Y)=H(Y)-H(Y\mid X), \tag{H.17}
$$

$$
I(X,Y)=H(X,Y)-[H(X\mid Y)+H(Y\mid X)]. \tag{H.18}
$$

### Channel Capacity / 信道容量

Channel capacity is the largest mutual information attainable by choosing the input distribution:

> 信道容量是通过选择输入分布所能达到的最大互信息：

$$
C=\max_{p(X)}I(X,Y). \tag{H.19}
$$

### Marginalisation / 边缘化

For discrete variables,

> 对离散变量：

$$
p(x_i)=\sum_j p(x_i,y_j), \qquad p(y_j)=\sum_i p(x_i,y_j). \tag{H.20-H.21}
$$

For continuous variables,

> 对连续变量：

$$
p(x)=\int_{\mathcal Y}p(x,y)\,dy, \qquad
p(y)=\int_{\mathcal X}p(x,y)\,dx. \tag{H.22}
$$

